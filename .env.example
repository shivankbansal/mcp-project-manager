# MCP Project Manager - Environment Variables

# Server Configuration
PORT=10000
NODE_ENV=development
MCP_SERVER_HOST=0.0.0.0

# Database (Optional - uses in-memory storage if not set)
MONGODB_URI=

# AI Providers (Configure at least one)
#
# Groq (Recommended - FREE & Fast)
# Get your key at: https://console.groq.com/keys
GROQ_API_KEY=

# Ollama (Self-hosted - FREE)
# Install: https://ollama.com/download
# Enable and configure local models
OLLAMA_ENABLED=
OLLAMA_MODEL=llama3.2
OLLAMA_HOST=http://localhost:11434

# OpenAI (Paid - Highest Quality)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Google Gemini (Free Tier Available)
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# Anthropic Claude (Future Support)
ANTHROPIC_API_KEY=

# ===== Security Settings =====

# CORS whitelist (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://devtrifecta-ui.onrender.com

# Admin token for protected operations (DELETE workflows, etc.)
ADMIN_TOKEN=your-secure-random-admin-token-here

# JWT Authentication
JWT_SECRET=your-jwt-secret-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ===== Responsible AI Governance =====

# Provider allowlist (comma-separated)
ALLOWED_PROVIDERS=openai,gemini,groq,ollama

# Model allowlists per provider
ALLOWED_MODELS_OPENAI=gpt-4o,gpt-4o-mini,gpt-4-turbo
ALLOWED_MODELS_GEMINI=gemini-2.0-flash-exp,gemini-1.5-flash,gemini-1.5-pro
ALLOWED_MODELS_GROQ=llama-3.3-70b-versatile,mixtral-8x7b-32768
ALLOWED_MODELS_OLLAMA=llama3.2,qwen2.5,mistral

# Safety features
ENABLE_SAFETY_CHECKS=true          # Enable content filtering (PII, credentials, malware, harmful)
REQUIRE_PURPOSE=false              # Require purpose field (disable for backwards compatibility)
REQUIRE_AUP=false                  # Require AUP acceptance (set true when UI is ready)

# Rate limits
GEN_RATE_LIMIT_PER_HOUR=20        # Max AI generations per hour per user/IP
GEN_RATE_LIMIT_PER_DAY=100        # Max AI generations per day per user/IP

# Audit logging
AUDIT_LOG_RETENTION_DAYS=90       # How long to keep audit logs
